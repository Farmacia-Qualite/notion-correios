{
  "name": "notion-correios",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Webhook que emite etiquetas dos Correios (CWS) a partir de páginas da DB - Vendas no Notion.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vercel dev",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.658.1",
    "@notionhq/client": "^2.2.15"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "@vercel/node": "^3.2.8",
    "typescript": "^5.6.2"
  }
}
